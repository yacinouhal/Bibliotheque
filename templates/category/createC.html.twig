<h1>Create Category</h1>
{% block body %}
    {{ form_start(monFormulaire) }}
        {{ form_widget(monFormulaire) }}

        <input type="submit" value="create Category" formnovalidate>
    {{ form_end(monFormulaire) }}
{% endblock %}
